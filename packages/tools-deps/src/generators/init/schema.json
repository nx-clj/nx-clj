{
  "$schema": "http://json-schema.org/schema",
  "$id": "Init",
  "title": "",
  "type": "object",
  "properties": {
    "depsProjectsPath": {
      "type": "string",
      "description": "Path under which all deps projects will be placed. If not provided, deps projects will be created under the default libraries folder"
    }
  },
  "required": []
}
